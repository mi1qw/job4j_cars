package com.example.car.model;

import static org.junit.jupiter.api.Assertions.*;

import lombok.extern.slf4j.Slf4j;
import org.junit.jupiter.api.Test;

import java.util.EnumMap;
import java.util.Map;

import static org.assertj.core.api.Assertions.*;

@Slf4j
class SortTest {

 /*   @Test
    void values() {
        log.info("{}", Sort.values());
        log.info("{}", Sort.priceExpensiveToCheap);
        log.info("{}", Sort.values());
        log.info("{}", Sort.timeNewToOld.ordinal());
        log.info("{}", Sort.priceExpensiveToCheap);


        EnumMap enumMap = new EnumMap<>(
                Map.of(
                        Sort.timeNewToOld, "Сначала новые",
                        Sort.priceExpensiveToCheap, "Сначала дорогие",
                        Sort.priceCheapToExpensive, "Сначала дешёвые"
                )
        );
        log.info("{}", enumMap);
    }*/


}